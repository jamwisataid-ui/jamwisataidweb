import Link from "next/link";
import { listArticlesAdmin } from "@/lib/cms/admin";
export default async function ArticlesAdminPage() { const items = await listArticlesAdmin(); return <><header className="admin-page-header"><div><p className="admin-eyebrow">EDUKASI JAMAAH</p><h1>Artikel & panduan</h1><p>Bangun kepercayaan melalui informasi yang bermanfaat dan mudah dipahami.</p></div><Link className="admin-primary-button" href="/admin/artikel/baru">Tulis Artikel</Link></header><section className="admin-panel"><div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Artikel</th><th>Status</th><th>Terakhir diubah</th><th /></tr></thead><tbody>{items.map((item) => <tr key={item.id}><td><strong>{item.title}</strong><small>/{item.slug}</small></td><td><span className={`admin-status ${item.status}`}>{item.status}</span></td><td>{item.updatedAt.toLocaleDateString("id-ID")}</td><td><Link href={`/admin/artikel/${item.id}`}>Edit →</Link></td></tr>)}</tbody></table></div></section></>; }
